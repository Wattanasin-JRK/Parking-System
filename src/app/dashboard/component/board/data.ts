export interface CarStats {
  id?: any;
  created: number;
  num : number;

  /** optional ไว้แสดงหรือซ่อน Form แก้ไข */
  active?: boolean;
}
export interface Total {
  id?: any;
  Tnum: Number;
  /** optional ไว้แสดงหรือซ่อน Form แก้ไข */
  active?: boolean;
}